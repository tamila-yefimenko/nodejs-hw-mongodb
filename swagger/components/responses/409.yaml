description: Email is already in use
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: Conflict. Email is use
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: Email is use
